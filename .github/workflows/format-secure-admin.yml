name: Format secure admin branch

on:
  push:
    branches: [feature/bg-en-admin-control-center]

permissions:
  contents: write

concurrency:
  group: format-secure-admin
  cancel-in-progress: true

jobs:
  format:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feature/bg-en-admin-control-center
          fetch-depth: 0
      - uses: oven-sh/setup-bun@v2
      - run: bun install --frozen-lockfile
      - name: Format secure admin scope
        run: |
          bunx prettier --write \
            src/components/admin/AdminAuthGate.tsx \
            src/integrations/admin/client.ts \
            src/lib/env.ts \
            src/routes/admin.data.tsx \
            src/server.ts \
            .github/workflows/ui-ux-ci.yml \
            .github/workflows/format-secure-admin.yml
          bunx eslint --fix \
            src/components/admin/AdminAuthGate.tsx \
            src/integrations/admin/client.ts \
            src/lib/env.ts \
            src/routes/admin.data.tsx \
            src/server.ts
      - name: Commit formatter output
        run: |
          if git diff --quiet; then
            echo "Already formatted"
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add \
            src/components/admin/AdminAuthGate.tsx \
            src/integrations/admin/client.ts \
            src/lib/env.ts \
            src/routes/admin.data.tsx \
            src/server.ts \
            .github/workflows/ui-ux-ci.yml \
            .github/workflows/format-secure-admin.yml
          git commit -m "style: format secure admin gateway"
          git push origin HEAD:feature/bg-en-admin-control-center
